# 📁 Структура проекта

```
chop_chop_chel_bot/
├── .github/
│   └── workflows/
│       └── deploy.yml              # GitHub Actions для автодеплоя
├── src/
│   ├── app/
│   │   ├── bot/                    # Telegram бот
│   │   │   ├── scenes/             # Сцены бота (booking, services, etc.)
│   │   │   ├── common/             # Общие компоненты (клавиатуры)
│   │   │   ├── helpers/            # Вспомогательные функции
│   │   │   ├── bot.module.ts
│   │   │   └── bot.service.ts
│   │   ├── booking/                # Бизнес-логика записей
│   │   ├── user/                   # Управление пользователями
│   │   └── yclients/               # Интеграция с YClients API
│   ├── db/                         # База данных
│   │   ├── schema.ts               # Схема БД (Drizzle ORM)
│   │   ├── db.module.ts
│   │   └── db.service.ts
│   ├── middleware/                 # Middleware (логирование, etc.)
│   ├── app.module.ts               # Главный модуль
│   ├── env.validator.ts            # Валидация переменных окружения
│   └── main.ts                     # Точка входа
├── migration/                      # SQL миграции
├── docs/                           # Документация
├── Dockerfile                      # Docker образ для бота
├── docker-compose.yml              # Docker для локальной разработки
├── docker-compose.prod.yml         # Docker для продакшена
├── package.json
├── tsconfig.json
├── drizzle.config.ts               # Конфигурация Drizzle ORM
├── biome.json                      # Линтер и форматтер
├── .env.example                    # Пример переменных окружения
├── DEPLOY_CHECKLIST.md             # 📋 Чеклист для деплоя
├── QUICK_START.md                  # ⚡ Быстрое руководство
├── DEPLOY.md                       # 📖 Полная документация
└── README.md                       # Этот файл
```

## 🔑 Ключевые файлы

### Конфигурация деплоя

- **`.github/workflows/deploy.yml`** - Автоматический деплой при push в main/master
- **`Dockerfile`** - Сборка Docker образа с приложением
- **`docker-compose.prod.yml`** - Запуск в продакшене (bot + PostgreSQL)
- **`.env`** - Переменные окружения (не в git!)

### Код приложения

- **`src/main.ts`** - Точка входа, запуск NestJS приложения
- **`src/app.module.ts`** - Главный модуль, импорт всех модулей
- **`src/app/bot/scenes/`** - Логика сцен бота (запись, услуги, записи)
- **`src/db/schema.ts`** - Схема базы данных

### Документация

- **`DEPLOY_CHECKLIST.md`** - Пошаговый чеклист с галочками для деплоя
- **`QUICK_START.md`** - Краткое руководство для быстрого старта
- **`DEPLOY.md`** - Подробная документация по деплою и настройке
- **`docs/architecture.md`** - Описание архитектуры проекта

## 🎯 Основные модули

### BotModule

Telegram бот на Telegraf. Обрабатывает команды, сцены, inline-кнопки.

**Сцены:**

- `booking.scene.ts` - Запись на услугу
- `services.scene.ts` - Просмотр услуг и цен
- `records.scene.ts` - Просмотр записей пользователя

### BookingModule

Бизнес-логика создания и управления записями через YClients API.

### UserModule

Управление пользователями: регистрация, сохранение контактов, токенов YClients.

### YClientsModule

HTTP-клиент для работы с YClients API v2.0:

- Получение услуг
- Получение мастеров
- Получение доступных слотов времени
- Создание записей

### DBModule

Работа с PostgreSQL через Drizzle ORM. Поддержка миграций.

## 🗄️ База данных

**Таблица `users`:**

- `id` - внутренний ID
- `telegram_id` - ID пользователя в Telegram
- `first_name`, `second_name`, `nickname` - данные пользователя
- `yclients_phone`, `yclients_email` - контакты для записи
- `yclients_user_token`, `yclients_user_id` - токен авторизации YClients
- `in_whitelist` - доступ к боту
- `created_at`, `updated_at` - метки времени

## 🔄 CI/CD

При push в `main` или `master`:

1. GitHub Actions подключается к VPS по SSH
2. Выполняет `git pull` на VPS
3. Пересобирает и перезапускает Docker контейнеры
4. Очищает старые образы

## 🛠️ Разработка

```bash
# Установка зависимостей
pnpm install

# Запуск в режиме разработки
pnpm start:dev

# Сборка
pnpm build

# Линтинг
pnpm lint

# Генерация миграций
pnpm db:generate
```

## 📦 Продакшн

```bash
# Запуск через Docker Compose
docker compose -f docker-compose.prod.yml up -d --build

# Просмотр логов
docker compose -f docker-compose.prod.yml logs -f bot

# Перезапуск
docker compose -f docker-compose.prod.yml restart bot
```

---

Больше информации: [README.md](./README.md)
