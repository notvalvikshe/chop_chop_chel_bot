# 📁 Структура проекта

```
chop_chop_chel_bot/
├── .github/
│   └── workflows/
│       └── deploy.yml              # GitHub Actions для автодеплоя
├── src/
│   ├── app/
│   │   ├── bot/                    # Telegram бот
│   │   │   ├── scenes/             # Сцены бота (booking, services, etc.)
│   │   │   ├── keyboards/          # Клавиатуры (main, inline)
│   │   │   ├── common/             # Общие компоненты
│   │   │   ├── helpers/            # Вспомогательные функции
│   │   │   ├── bot.module.ts
│   │   │   └── bot.service.ts
│   │   ├── booking/                # Бизнес-логика записей
│   │   ├── notification/           # Сервис уведомлений (рассылка сообщений)
│   │   ├── settings/               # Настройки бота (key-value хранилище)
│   │   ├── user/                   # Управление пользователями
│   │   └── ...
│   ├── yclients/                   # Интеграция с YClients API
│   │   ├── yclients.module.ts
│   │   ├── yclients-api.service.ts
│   │   └── yclients.types.ts
│   ├── db/                         # База данных
│   │   ├── schema.ts               # Схема БД (Drizzle ORM)
│   │   ├── db.module.ts
│   │   └── db.service.ts
│   ├── middleware/                 # Middleware (логирование, etc.)
│   ├── app.module.ts               # Главный модуль
│   ├── env.validator.ts            # Валидация переменных окружения
│   └── main.ts                     # Точка входа
├── migration/                      # SQL миграции
├── docs/                           # Документация
├── Dockerfile                      # Docker образ для бота
├── docker-compose.yml              # Docker для локальной разработки
├── docker-compose.prod.yml         # Docker для продакшена
├── package.json
├── tsconfig.json
├── drizzle.config.ts               # Конфигурация Drizzle ORM
├── biome.json                      # Линтер и форматтер
├── .env.example                    # Пример переменных окружения
├── DEPLOY_CHECKLIST.md             # 📋 Чеклист для деплоя
├── QUICK_START.md                  # ⚡ Быстрое руководство
├── DEPLOY.md                       # 📖 Полная документация
└── README.md                       # Этот файл
```

## 🔑 Ключевые файлы

### Конфигурация деплоя

- **`.github/workflows/deploy.yml`** - Автоматический деплой при push в main/master
- **`Dockerfile`** - Сборка Docker образа с приложением
- **`docker-compose.prod.yml`** - Запуск в продакшене (bot + PostgreSQL)
- **`.env`** - Переменные окружения (не в git!)
  - `TELEGRAM_TOKEN` - Токен Telegram бота
  - `DATABASE_URL` - Строка подключения к PostgreSQL
  - `YCLIENTS_PARTNER_TOKEN` - Партнерский токен YClients
  - `DEFAULT_ADMIN` - Ссылка на чат с админом по умолчанию

### Код приложения

- **`src/main.ts`** - Точка входа, запуск NestJS приложения
- **`src/app.module.ts`** - Главный модуль, импорт всех модулей
- **`src/app/bot/scenes/`** - Логика сцен бота (запись, услуги, записи)
- **`src/db/schema.ts`** - Схема базы данных

### Документация

- **`DEPLOY_CHECKLIST.md`** - Пошаговый чеклист с галочками для деплоя
- **`QUICK_START.md`** - Краткое руководство для быстрого старта
- **`DEPLOY.md`** - Подробная документация по деплою и настройке
- **`docs/architecture.md`** - Описание архитектуры проекта

## 🎯 Основные модули

### BotModule

Telegram бот на Telegraf. Обрабатывает команды, сцены, inline-кнопки.

**Основные команды:**

- `/start` - Главное меню с динамической кнопкой админа
- `/services` - Просмотр услуг и цен
- `/book` - Запись на услугу через сцену
- `/my_bookings` - Мои записи
- `📞 Позвать админа` - Ссылка на чат с админом
- Обработка неизвестных сообщений

**Сцены:**

- `booking.scene.ts` - Запись на услугу
- `services.scene.ts` - Просмотр услуг и цен
- `records.scene.ts` - Просмотр записей пользователя

### BookingModule

Бизнес-логика создания и управления записями через YClients API.

### SettingsModule

Key-value хранилище настроек бота в БД с fallback на переменные окружения.

**Возможности:**

- Хранение ссылки на админа
- Динамическое управление настройками
- Fallback на DEFAULT_ADMIN из env

### NotificationModule

Сервис для отправки сообщений пользователям с поддержкой отложенной отправки.

**Возможности:**

- Отправка сообщений по массиву ID пользователей
- Отложенная отправка с указанием даты/времени
- Поддержка Markdown, HTML разметки
- Batch отправка с учётом Telegram rate limit
- Отмена запланированных сообщений
- Broadcast всем пользователям или только whitelist
- Отслеживание результатов отправки

### UserModule

Управление пользователями: регистрация, сохранение контактов, токенов YClients.

### YClientsModule

HTTP-клиент для работы с YClients API v2.0:

- Получение услуг
- Получение мастеров
- Получение доступных слотов времени
- Создание записей

### DBModule

Работа с PostgreSQL через Drizzle ORM. Поддержка миграций.

## 🗄️ База данных

**Таблица `users`:**

- `id` - внутренний ID
- `telegram_id` - ID пользователя в Telegram
- `first_name`, `second_name`, `nickname` - данные пользователя
- `yclients_phone`, `yclients_email` - контакты для записи
- `yclients_user_token`, `yclients_user_id` - токен авторизации YClients
- `in_whitelist` - доступ к боту
- `created_at`, `updated_at` - метки времени

**Таблица `bookings`:**

- `id`, `user_id`, `service_id`, `staff_id` - основные поля
- `datetime`, `status`, `phone` - данные записи
- `company_id`, `company_name` - информация о компании
- `created_at`, `updated_at` - метки времени

**Таблица `settings`:**

- `id` - первичный ключ
- `key` - уникальный ключ настройки
- `value` - значение настройки
- `created_at`, `updated_at` - метки времени

## 🔄 CI/CD

При push в `main` или `master`:

1. GitHub Actions подключается к VPS по SSH
2. Выполняет `git pull` на VPS
3. Пересобирает и перезапускает Docker контейнеры
4. Очищает старые образы

## 🛠️ Разработка

```bash
# Установка зависимостей
pnpm install

# Запуск в режиме разработки
pnpm start:dev

# Сборка
pnpm build

# Линтинг
pnpm lint

# Генерация миграций
pnpm db:generate
```

## 📦 Продакшн

```bash
# Запуск через Docker Compose
docker compose -f docker-compose.prod.yml up -d --build

# Просмотр логов
docker compose -f docker-compose.prod.yml logs -f bot

# Перезапуск
docker compose -f docker-compose.prod.yml restart bot
```

---

## 📝 Обновление документации

**ВАЖНО:** При каждом изменении кода обязательно обновляйте документацию!

### Что обновлять:

1. **PROJECT_STRUCTURE.md** — при добавлении новых модулей, файлов или изменении структуры
2. **docs/architecture.md** — при изменении архитектуры, добавлении сервисов, новой логики
3. **README.md** — при изменении команд запуска, требований, основного функционала
4. **.env.example** — при добавлении новых переменных окружения
5. **DEPLOY.md** — при изменении процесса деплоя или конфигурации

### Процедура:

1. Внести изменения в код
2. Проверить работоспособность (`pnpm lint:check && pnpm tsc --noEmit`)
3. Обновить соответствующую документацию
4. Добавить информацию о новых возможностях в README или CHANGELOG
5. Закоммитить изменения с описательным сообщением

**Пример хорошего commit message:**

```
feat: add notification service for user messaging

- Added NotificationService with scheduled messaging
- Support for batch sending with rate limiting
- Updated docs/architecture.md and PROJECT_STRUCTURE.md
```

---

Больше информации: [README.md](./README.md)
