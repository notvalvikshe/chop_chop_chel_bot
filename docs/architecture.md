# t-bot — Architecture & Development Notes

Этот файл описывает текущую архитектуру проекта и договорённости по разработке. При изменении кода и добавлении новых фич опираемся на эти принципы.

## 1. Overview

- **Назначение**: Telegram‑бот для онлайн-записи на стрижку через YClients API.
- **Основные задачи**:
  - Принимать апдейты от Telegram через `nestjs-telegraf`.
  - Взаимодействовать с YClients API для управления записями, услугами и мастерами.
  - Вести учёт пользователей в PostgreSQL через Drizzle ORM.
  - Логировать входящие сообщения и SQL‑запросы.

## 2. Tech Stack

- **Runtime / Backend**: Node.js, NestJS.
- **DI / Модули**: стандартная модульная система Nest.
- **Telegram**: `nestjs-telegraf`, `telegraf`, `telegraf-session-local`.
- **База данных**: PostgreSQL + `drizzle-orm` (`node-postgres`).
- **Внешний API**: YClients REST API через `axios`.
- **Конфигурация**: `@nestjs/config` + `zod` + `nestjs-zod`.
- **Прочее**: `radash` (утилита `objectify`), `sql-highlight`.

## 3. Project Structure (high level)

- `src/main.ts` — точка входа Nest-приложения.
- `src/app.module.ts` — корневой модуль, собирает все остальные модули.
- `src/env.validator.ts` — схема и валидация переменных окружения.
- `src/app/bot` — Telegram‑бот (модуль, сервис, вспомогательные типы и утилиты).
- `src/app/booking` — домен бронирования (сервис для работы с записями).
- `src/yclients` — модуль для работы с YClients API (HTTP-клиент, типы).
- `src/app/user` — домен пользователя (сервис, репозиторий, модель).
- `src/db` — модуль базы данных, Drizzle‑сервис и схема таблиц.
- `src/middleware` — middleware, которые вешаются на Telegraf.

При добавлении новых доменов/фич желательно следовать той же структуре: отдельный модуль в `src/app/<domain>` с сервисом и репозиторием, по необходимости — сущности в `src/db/schema.ts`.

## 4. Application Bootstrap

- Точка входа: `bootstrap()` в `src/main.ts`.
- Создание приложения: `NestFactory.create(AppModule, { bufferLogs: true, logger: ['error', 'fatal', 'log', 'warn', 'debug'] })`.
- HTTP‑сервер слушает порт `3000` по умолчанию.
- Корневой модульYClientsModule`, `BotModule`, `DBModule`, `UserModule`, `Booking
  - Подключает `ConfigModule.forRoot` с кастомной валидацией через `validateSchema`.
  - Импортирует `BotModule`, `DBModule`, `UserModule`.
  - Делает `ConfigModule` глобальным (`isGlobal: true`).

## 5. Environment & Configuration

Файл: `src/env.validator.ts`.

- Используем `zod` для описания схемы окружения и `nestjs-zod` для генерации DTO.
- Основные переменные:
  - `ENV: string` — окружение (dev/prod и т.п.).
  - `PORT: string -> number` — порт HTTP‑сервера (по умолчанию `3000`).
  - `YCLIENTS_PARTNER_TOKEN` — партнёрский токен YClients API.
  - `YCLIENTS_BOOK_FORM_ID` — ID формы бронирования YClients.
  - `YCLIENTS_COMPANY_ID` — ID компании в YClients.
  - `DB_PORT, DB_USERNAME, DB_PASSWORD, DB_NAME, DATABASE_URL` — конфиг PostgreSQL (фактически используется `DATABASE_URL`).
  - `TELEGRAM_TOKEN` — токен Telegram‑бота.
- При старте приложения `ConfigModule` вызывает `validateSchema`, при ошибках в переменных окружения приложение не поднимается.

**Рекомендация**: при добавлении новых переменных окружения обязательно:

1. Добавить поле в `envSchema`.
2. При необходимости расширить тип `Env` и использовать `ConfigService<Env, true>`.

## 6. Telegram Bot Layer

Файлы: `src/app/bot/bot.module.ts`, `src/app/bot/bot.service.ts`, `src/app/bot/helpers/*`.

### 6.1 BotModule

- Импортирует `TelegrafModule.forRootAsync` и передаёт токен из `ConfigService`.
- Импортирует `UserModule` для доступа к `UserService`.
- Регистрирует `BotService` как обработчик апдейтов.

### 6.2 Типы контекста

Файл: `src/app/bot/helpers/bot-types.ts`.

- Расширение контекста Telegraf:
  - `MyContext` содержит:
    - `user: User` — текущий пользователь (объект доменной модели).
    - `session: Scenes.SceneSession` — сессия, используемая `telegraf-session-local` и/или сценами.
- При написании хендлеров следует использовать `MyContext` для доступа к `ctx.user` и `ctx.session` с типизацией.

### 6.3 BotService

Файл: `src/app/bot/bot.service.ts`.

- Класс с декоратором `@Update()` — основной обработчик Telegram‑апдейтов.
- В конструкторе:
  - Через `@InjectBot()` получает `Telegraf<MyContext>`.
  - Подключает middleware:
    - `attachUserMiddleware()` — привязка пользователя к `ctx.user` через `UserService`.
    - `LoggerMiddleware` — логирование входящих апдейтов.
    - `LocalSession` — локальное хранение сессий в файле `session.json`.
  - Настраивает команду `/start` через `setMyCommands`.
- Хендлер `/start` (`@Start()`):
  - Использует `ctx.user` для приветствия по имени.

### 6.4 Добавление новых команд/обработчиков

Рекомендованный порядок:

1. В `BotService` добавить новый метод с соответствующим декоратором:
   - `@Command('name')`, `@Hears`, `@On('message')` и т.п. из `nestjs-telegraf`.
2. Использовать `MyContext` в сигнатуре метода: `async onSomething(@Ctx() ctx: MyContext) { ... }`.
3. При необходимости:
   - Обратиться к `ctx.user` для данных пользователя.
   - Использовать `ctx.session` для хранения состояния.
4. При сложной логике вынести бизнес‑логику в отдельный сервис/класс в доменном модуле, а в `BotService` оставить только «тонкий» слой работы с Telegram API.

## 7. User Domain

Файлы: `src/app/user/user.module.ts`, `src/app/user/user.service.ts`, `src/app/user/user.repository.ts`, `src/app/user/user.ts`.

### 7.1 UserModule

- Регистрирует `UserService` и `UserRepository`.
- Экспортирует их, чтобы бота и другие модули могли работать с пользователями.

### 7.2 UserRepository

- Работает с `DBService` и `userSchema` (Drizzle ORM).
- Основные методы:
  - `create(userCreateData)` — upsert по `telegramId` (при конфликте обновляет имя и ник).
  - `findByTelegramId(telegramId)` — поиск по Telegram ID.
  - `findById(id)` — поиск по внутреннему ID.
  - `findByIds(ids)` — массовый выбор пользователей по ID.
- На выходе всегда возвращает доменную модель `User`.

### 7.3 UserService

- Обёртка над `UserRepository` — здесь описывается доменная логика.
- Основные методы:
  - `getUserByTelegramUser(telegramUser)` — принимает объект пользователя из Telegram, ищет в БД по `telegramId`, при отсутствии создаёт.
  - `getUserById(id)` — обёртка над репозиторием.
  - `getUserByIds(ids, options)` — возвращает либо массив, либо словарь `id -> User` (через `ResultType` и `objectify`).

### 7.4 Рекомендации по расширению домена пользователя

- Если требуется новая бизнес‑логика (whitelist, роли, настройки и т.п.):
  1. Добавить поля в `userSchema` (см. раздел 8).
  2. Обновить модель `User`.
  3. Добавить методы в `UserRepository` (поиск/обновление/фильтрация).
  4. Добавить высокоуровневые методы в `UserService`.
  5. В бот‑слое обращаться **только** к `UserService`, а не напрямую к `UserRepository`.

## 8. Database Layer

Файлы: `src/db/db.module.ts`, `src/db/db.service.ts`, `src/db/schema.ts`.

### 8.1 Schema

Файл: `src/db/schema.ts`.

- Описание таблицы `users` через Drizzle `pgTable`:
  - `id` — первичный ключ.
  - `telegramId` — уникальный ID пользователя Telegram.
  - `firstName`, `secondName`, `nickname` — данные профиля.
  - `createdAt`, `updatedAt` — временные метки, по умолчанию `now()`.
  - `inWhitelist` — флаг, используется для ограничений доступа.
  - **YClients auth fields** (добавлено для интеграции):
    - `yclientsUserToken` — токен пользователя YClients после SMS-авторизации.
    - `yclientsUserId` — ID пользователя в YClients.
    - `yclientsPhone` — телефон, с которым пользователь авторизован в YClients.

При добавлении новых сущностей рекомендуется:

1. Определить новую таблицу в этом файле (`pgTable`).
2. Экспортировать схему.
3. Использовать её в соответствующем репозитории.

### 8.2 DBModule & DBService

- `DBModule` помечен как `@Global()`, экспортирует `DBService`.
- `DBService` на `onModuleInit` инициализирует Drizzle ORM:
  - Использует `DATABASE_URL` из конфигурации.
  - Вешает кастомный логгер `MyLogger` для логирования SQL.
- Поле `db` — инстанс `NodePgDatabase` с доступом к низкоуровневому клиенту (`$client`).

### 8.3 DB Logging

- `MyLogger` форматирует запросы, подставляя значения параметров (`$1`, `$2` и т.д.) и подсвечивая SQL через `sql-highlight`.
- Логгер пишет на уровень `debug` через Nest `Logger`.

## 9. Telegram Logging Middleware

Файл: `src/middleware/logger.middleware.ts`.

- `LoggerMiddleware` — Telegraf‑middleware, а не Nest‑middleware.
- Логирует:
  - Тип и содержимое входящего сообщения (text, photo, video, voice, document, sticker, location, contact, poll, animation и т.п.).
  - `callback_query` с данными колбэка.
  - `inline_query` с текстом запроса.
- Для лога формируется человекочитаемая строка с пользователем: `👤 {name} ({id})`.

Используется в конструкторе `BotService` через `this.bot.use(new LoggerMiddleware().middleware())`.

## 10. Typical Telegram Update Flow

1. Telegram отправляет апдейт.
2. Telegraf (через `nestjs-telegraf`) передаёт апдейт в Nest.
3. В `BotService` срабатывает цепочка middleware:
   - `attachUserMiddleware` — находит/создаёт пользователя, помещает его в `ctx.user`.
   - `LoggerMiddleware` — логирует апдейт.
   - `LocalSession` — поднимает/сохраняет сессию.
4. Если апдейт соответствует одному из декораторов в `BotService` (например, `/start`, `/services`, `/book`), вызывается соответствующий метод.
5. Метод хендлера использует `ctx`, доменные сервисы (`UserService`, `BookingService` и др.) и отправляет ответ через Telegram API.

## 10a. YClients Integration Layer

Файлы: `src/yclients/yclients.module.ts`, `src/yclients/yclients-api.service.ts`, `src/yclients/yclients.types.ts`.

### 10a.1 YClientsModule

- Помечен как `@Global()` — доступен во всём приложении.
- Экспортирует `YClientsApiService` для использования в других модулях.

### 10a.2 YClientsApiService

HTTP-клиент для работы с YClients REST API (базовый URL: `https://api.yclients.com`).

**Основные методы:**

- **Auth**: `startAuth(phone)`, `confirmAuth(phone, code, uuid)` — SMS-авторизация пользователей.
- **Companies**: `getCompany(companyId)` — получение информации о компании.
- **Services**: `listServiceCategories(companyId)`, `listServices(companyId, categoryId?)` — список категорий и услуг.
- **Staff**: `listStaff(companyId, serviceIds?)` — список мастеров.
- **Free Times**: `getFreeTimes(params)` — доступные временные слоты для записи.
- **Records**: `createRecord(request, userToken?)`, `getUserRecords(params, userToken)`, `cancelRecord(recordId, userToken)`, `rescheduleRecord(recordId, datetime, userToken)`.

**Авторизация:**

- Партнёрский токен (`YCLIENTS_PARTNER_TOKEN`) добавляется ко всем запросам.
- Для операций от имени пользователя (мои записи, отмена) также передаётся `userToken` в заголовке `Authorization: Bearer <partner_token>, User <user_token>`.

**Логирование:**

- Все запросы и ответы логируются через Nest Logger на уровне `debug`.
- Ошибки API логируются на уровне `error` с деталями ответа.

### 10a.3 YClients Types

Файл `src/yclients/yclients.types.ts` содержит TypeScript-интерфейсы для всех сущностей YClients API согласно `docs/apidoc.md`:

- `Company`, `Service`, `ServiceCategory`, `Staff`, `Record`, `Client` и др.
- Request/Response типы для всех эндпоинтов.

## 10b. Booking Domain

Файлы: `src/app/booking/booking.module.ts`, `src/app/booking/booking.service.ts`.

### 10b.1 BookingModule

- Регистрирует `BookingService`.
- Экспортирует его для использования в `BotService`.

### 10b.2 BookingService

Сервис-обёртка над `YClientsApiService` для упрощения работы с бронированием.

**Основные методы:**

- `getAvailableServices()` — получить список услуг, доступных для онлайн-записи (фильтр `allow_online = true`).
- `getServicesByCategory(categoryId)` — получить услуги по категории.
- `getStaffForService(serviceId)` — получить список мастеров для конкретной услуги.
- `getAvailableSlots(serviceId, staffId?, date)` — получить свободные временные слоты на дату.
- `createBooking(serviceId, staffId, datetime, clientName, clientPhone, userToken?)` — создать запись.
- `getUserBookings(userToken, from?, to?)` — получить записи пользователя.
- `cancelBooking(recordId, userToken)` — отменить запись.
- `rescheduleBooking(recordId, newDatetime, userToken)` — перенести запись.

**Использование:**

- `BookingService` автоматически использует `YCLIENTS_COMPANY_ID` из конфигурации.
- При необходимости user-токена (мои записи, отмена) он передаётся явно в методы.

## 10c. Bot Commands

Текущие реализованные команды в `BotService`:

### `/start`

- Приветствие пользователя с главным меню.
- Показывает клавиатуру с кнопками: "Мои записи", "Записаться", "Услуги и цены", "Помощь".

### `/services` или кнопка "💇 Услуги и цены"

- Запрашивает список услуг через `BookingService.getAvailableServices()`.
- Отображает название, цену и длительность каждой услуги.

### `/book` или кнопка "📅 Записаться"

- Запускает интерактивную сцену записи (BookingScene).
- Пошаговый выбор: услуга → мастер → дата → время.
- Сбор контактных данных (телефон, email) при первой записи.
- Подтверждение и создание записи через YClients API.
- Возможность выхода из сцены при нажатии кнопок главного меню.

### `/my_bookings` или кнопка "📋 Мои записи"

- Проверяет наличие `yclientsUserToken` у пользователя.
- Если токен есть — запрашивает записи через `BookingService.getUserBookings()`.
- Показывает список будущих записей с датой, временем, услугой и ID.

### `/help` или кнопка "ℹ️ Помощь"

- Справка по доступным командам.

## 11. Coding Conventions for This Project

При работе над кодом ориентируемся на следующие принципы:

- **NestJS‑стиль**:
  - Разделение на модули/сервисы/репозитории.
  - Внутри слоя бота не писать тяжёлую бизнес‑логику — выносить в сервисы домена.
- **TypeScript**:
  - Не использовать однобуквенные имена переменных, кроме краткоживущих счётчиков в циклах.
  - Стараться указывать явные типы для публичных методов сервисов и репозиториев.
- **DI**:
  - Зависимости передавать через конструктор.
  - Для кросс‑модульного использования экспортировать сервисы и репозитории из модулей.
- **БД‑доступ**:
  - Всегда работать с базой через `DBService.db` и схемы Drizzle.
  - Вынести любую работу с SQL в репозитории, не ходить в БД напрямую из бот‑слоя.
- **Ошибки и валидация**:
  - Валидация входных данных конфигурации — через Zod (`envSchema`).
  - При проверке пользовательского ввода в командах можно использовать либо собственные guard/проверки, либо выделенные валидаторы.
- **Логирование**:
  - Для бизнес‑логики использовать Nest‑логгер (`new Logger(ClassName.name)`).
  - Не злоупотреблять `log`/`debug`, но логировать ключевые действия и ошибки.

## 12. How to Extend the Project

### 12.1 Новая команда бота

1. Определить сценарий и какие доменные сервисы нужны.
2. При необходимости доработать доменный модуль (сервис/репозиторий/схема БД).
3. В `BotService` добавить метод с нужным декоратором (`@Command`, `@Hears`, `@On` и т.д.).
4. В хендлере использовать `ctx.user`, `ctx.session`, доменные сервисы.
5. При необходимости добавить команду через `setMyCommands`.

### 12.2 Новая доменная сущность (например, Orders)

1. Добавить таблицу `orders` в `src/db/schema.ts`.
2. Создать модуль `OrderModule` в `src/app/order`:
   - `order.module.ts`, `order.service.ts`, `order.repository.ts`, `order.ts` (модель).
3. В `OrderRepository` использовать Drizzle‑схемы для работы с таблицей.
4. В `OrderService` описать бизнес‑логику.
5. Подключить `OrderModule` в `AppModule` и использовать его в `BotService` или других модулях.

### 12.3 Новые переменные окружения

1. Добавить поле в `envSchema` в `src/env.validator.ts`.
2. При необходимости обновить тип `Env`.
3. Использовать `ConfigService<Env, true>` для доступа к новой переменной.

---

Этот файл служит основным источником архитектурной информации для разработки. При существенных изменениях в архитектуре или паттернах работы с кодом рекомендуется сначала обновить этот документ, а затем вносить изменения в кодовую базу.
